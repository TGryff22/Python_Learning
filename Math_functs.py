x = 3.14
y = -8
z = 6

# result = round(x)  round x to the nearest integer
# result = abs(y) # absolute value of y. How far away from zero y is
# result = pow(z, 3) #, 2) # z to the power of 3, and then round to 2 decimal places
# result = max(x, y, z) # find the maximum value among x, y, and z
result = min(x, y, z) # find the minimum value among x, y, and z

print(result)